/*
 * AUTO-GENERATED BY verify-equiv v0.
 *
 * Do not treat __ESBMC_PY_TARGET as an implementation.
 * The relational bridge retargets this placeholder to the
 * real Python GOTO function after binary merge.
 */

extern int __VERIFIER_nondet_int(void);

extern long __ESBMC_PY_TARGET(long x, long y);
extern int __ESBMC_C_TARGET(int x, int y);

int equiv_main(void)
{
    int x = __VERIFIER_nondet_int();
    int y = __VERIFIER_nondet_int();

    long r_py = (long)__ESBMC_PY_TARGET((long)x, (long)y);
    long r_c  = (long)__ESBMC_C_TARGET(x, y);

    __ESBMC_assert(
        r_py == r_c,
        "__VERIFY_EQUIV_RELATIONAL_PROPERTY__"
    );

    return 0;
}

int main(void)
{
    return equiv_main();
}
